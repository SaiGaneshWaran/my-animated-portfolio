// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  compiler: {
    // This enables the styled-components compiler for better performance and debugging
    styledComponents: true,
  },
};

export default nextConfig;